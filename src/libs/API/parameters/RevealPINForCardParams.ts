import type {MultifactorAuthenticationAPIParams} from '@components/MultifactorAuthentication/config/types';

type RevealPINForCardParams = MultifactorAuthenticationAPIParams<'REVEAL-PIN'>;

export default RevealPINForCardParams;
